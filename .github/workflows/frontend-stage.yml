name: Deploy frontend

on: workflow_call

#Steps
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Build frontend
        run: echo Собирается билд фронта \(прогоняются юнит тесты, запускаются линтеры\), формируется и публикуется докер образ с изменениями

  deploy-stage:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy frontend to stage/review app
        run: echo Происходит деплой фронта